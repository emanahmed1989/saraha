
// we don't need it in express 5
// export const asyncHandler = function (fn) {

//     return (req, res, next) => {
//         fn(req, res, next).catch.catch((error) => {
//             next(error)
            
//         });
//     }

// }